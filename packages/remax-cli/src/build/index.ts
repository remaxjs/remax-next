import { logger } from '@remax/utils';
import type { Options } from '@remax/types';
import API from '../api';

export function internalBuildApp(options: Options, api: API) {
  logger.info('🚀 构建应用');
}
